// horizontal
// diagonal
// vertical

// adjacent hex pieces

export function getStraightMovesUp(position: string) {
	const letter = position.charAt(0)
	const number = position.slice(1)

	let moves = []

	let currentPosition = position
}
